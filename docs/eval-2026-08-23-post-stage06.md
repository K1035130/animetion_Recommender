# 阶段 06 收口评测（2026-08-23）

语料层冻结后的一轮评测。**本文只报客观可判的部分（工作流 ①）**；
60 题可回答率是人工打分，表在 `docs/eval-answerability-rescore-v4.md`，填完跑
`eval_answerability.py score`。

## 0. 口径与指纹（🚨 先读这一节）

**Kevin 定（2026-08-23）：不重跑生成侧，在报告里注明指纹和日期。**

| | 基线 | 本次 |
|---|---|---|
| 日期 | 2026-08-19 | 2026-08-23 |
| `llm.descriptor().fingerprint` | `bc6cdf288dd69017` (55 题) | `fc6fbd634f6367d8` (56 题) |
| `prompts` 摘要 | `07cc60bcd5216704` | `e3c118046d0a2046` |
| 语料 | `plot_chunk` 120,481 | **135,534**（角色页 30,534 → 45,587） |

🚨 **两侧的 `fingerprint` 与 `prompts` 都不同 ⇒ 生成侧的数字不可直接并排比较。**
（None 的那几题是短路返回，压根没调 LLM：state≠ok 或没语料。）
✅ **检索侧可以比**：embedding 指纹未变，题面经 `--only` 逐条核对一致。

## 1. 150 题点名类检索（自动标注，客观可判）

`eval_retrieval_named.py --n 150 --seed 0`，与 week5 同一批题。

| | week5 基线 | 本次 | Δ |
|---|---|---|---|
| 解析 ok | 88.7% | **88.7%** | 0 |
| ambiguous | 8.0% | **8.0%** | 0 |
| 作用域错 | 3.3% | **3.3%** | 0 |
| **直取=开（线上形态）** | **100%** | **100%** | **0** |
| 直取=关（纯召回消融） | 28.6% | **27.1%** | −1.5pp |
| 同上，`MIN_SCORE=0` | 51.1% | **46.6%** | −4.5pp |

**✅ 线上形态 100%，未受任何影响**；解析层三个数逐条不变（语料改动不碰解析层，
符合预期 —— 那 11.3% 的失败是 alias 的事，见 week5 §3.1）。

### ⚠️ 纯召回那两列降了，但**不能读成"质量下降"**

两条理由，第一条是定义问题、第二条有实测：

1. 🚨 **这个评测的标准答案只认 `source='bangumi_char'` 的 chunk**
   （出题时 gold = 该 `character_id` 的 dump 简介）。萌娘角色页**即使答对
   也算未命中** —— 而阶段 06 加的正好全是萌娘角色页。⇒ 分母口径决定了
   新语料只会把这个指标往下压。
2. **探针实测（18 题，前 8 席位 136 个，关掉直取的纯召回形态）**：

   ```
   dump 角色简介      105   77.2%
   萌娘 character 页   16   11.8%   ← 新语料实际占了这些席位
   萌娘 series 页      15   11.0%
   ```
   10 道未命中题里 **3 道**前 8 含萌娘角色页。

⚠️ **挤占只解释了一部分，别把 4.5pp 全归给它** —— 另外 7 道未命中在基线时
就已经未命中（基线本身只有 28.6%）。**结论只到"方向一致、量级合理"为止。**

📌 **参数扫描的形状与 week5 一致，不据此改常量**：`MIN_SCORE` 仍是单调的
（0→46.6% / 0.05→27.1%），召回宽度与 final 仍在 40 / 8 附近饱和。
week5 §3.3 那条"不据此改常量，等 B.1 人工题库"**继续维持**。

## 2. 12 道结局题：检索侧新旧对照

阶段 06 的立项理由之一是 I.9「结局细节住在角色页」。第一批（热度前 500）
实测 0/12 → **2 完整 + 1 部分**。第二批（前 1000 + 2021 年以来）之后：

```
检索结果发生变化   7/12 题
新进入的 chunk    31 条（合计）
```

⚠️ **但按粗判（关键词扫 answer 开头，不是打分）只有 2 题在作答**，
仍是 [19] 命运石之门 与 [24] CLANNAD —— **和第一批那两题是同两题**。
⇒ **第二批对结局题的边际收益看起来接近 0**，与「覆盖率一点没涨」互相印证：
第二批加的是**已覆盖作品的深度**，而这 12 道题问的是另一批作品。

🚨 **这一段是粗判不是评分**，真值以 v4 打分表为准。

📌 **[22]《总之就是非常可爱 ～制服～》`moe=0`** —— 一条萌娘 chunk 都没有，
正是第四部分「同 IP 衍生折叠」那个缺口的活体样本：`series_root=376708`
拿不到主系列 `301541` 的语料。**抓多少角色页都救不了它**，得改 series_root。

## 3. 待办

- ⬜ v4 打分表待填 → `eval_answerability.py score`
- ⬜ 若要让「纯召回」这个指标继续有意义，出题时的 gold 应扩成
  「该角色的所有 chunk（含萌娘角色页）」，否则语料越全、这个数越难看。
  ⚠️ 改了就与 week5 不可比，**要么不改，要么两个口径都报**。
