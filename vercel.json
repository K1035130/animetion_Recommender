{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "regions": ["iad1"],
  "functions": {
    "api/index.py": {
      "maxDuration": 30,
      "includeFiles": "{server,src}/**/*.py"
    }
  }
}
